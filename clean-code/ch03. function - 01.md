#SRP #OCP #Factory 
```csharp
public Money CalculatePay(Employee e)
{
    switch (e.Type)
    {
        case EmployeeType.Commissioned:
            return CalculateCommissionedPay(e);
            
        case EmployeeType.Hourly:
            return CalculateHourlyPay(e);
            
        case EmployeeType.Salaried:
            return CalculateSalariedPay(e);
            
        default:
            throw new InvalidEmployeeTypeException(e.Type);
    }
}
```

### **❗문제점**

- switch문은 CalculatePay() 함수 안에 급여 계산의 **세부사항을 노출**함.
- 하나의 함수에서 **여러 급여 정책의 상세 구현을 호출** → ==추상화 수준 섞임==.
- 새로운 타입 추가 시 매번 switch 수정 필요 → **OCP 위반(개방-폐쇄 원칙 위반)**

### OCP(open close principle)
> **“소프트웨어 요소는 확장에는 열려 있어야 하고, 변경에는 닫혀 있어야 한다.”**

- **기존 코드를 수정하지 않고도 새로운 기능을 추가할 수 있어야 한다.**
- → 코드 **수정 없이** 동작을 **확장 가능**하게 설계해야 한다.

```csharp
public abstract class Employee
{
    public abstract Money CalculatePay();
}

public class Freelancer : Employee
{
    public override Money CalculatePay() /* 프리랜서 계산 */;
}
```

# SOL
---
### 1. abstract class
```csharp
public abstract class Employee
{
    public abstract bool IsPayday();
    public abstract Money CalculatePay();
    public abstract void DeliverPay(Money pay);

}
```

### 2. impl of Employees as...
```csharp
public class CommissionedEmployee : Employee

{
    private EmployeeRecord record;
    public CommissionedEmployee(EmployeeRecord record) {
	    this.record = record;
    }

    public override bool IsPayday();
    public override Money CalculatePay();
    public override void DeliverPay(Money pay);
}
```


### 3. Factory Design Pattern
**“객체 생성을 담당하는 클래스를 별도로 분리”** 하는 설계 패턴입니다.

- 어떤 클래스(Employee, Shape, Button 등)를 **직접 new 하지 않고**,
- **팩토리(Factory)** 라는 객체에게 생성 책임을 위임합니다.

**이유는?**
- 생성 로직을 숨기고 싶거나,
- 타입별로 다양한 객체를 상황에 맞게 만들고 싶거나,
- switch/if 분기를 한 곳에만 모으고 싶을 때.

```csharp
public interface IEmployeeFactory
{
    Employee MakeEmployee(EmployeeRecord record);
}

public class EmployeeFactory : IEmployeeFactory
{
    public Employee MakeEmployee(EmployeeRecord record)
    {
        return record.Type switch
        {
            EmployeeType.Commissioned => new CommissionedEmployee(record),
            EmployeeType.Hourly => new HourlyEmployee(record),
            EmployeeType.Salaried => new SalariedEmployee(record),
            _ => throw new InvalidEmployeeTypeException(record.Type)

        };
    }
}
```

### 4. 만약에 유형 추가가 있는 경우
1. Employee 구현체만 추가하면 됨
2. MakeEmployee 팩토리 switch 문에 추가만 해주면 됨

**금액 정산하는 로직은 변하지 않음**
```csharp
public void ProcessPayroll(Employee employee)
{
    var pay = employee.CalculatePay();
}
```


### SRP(Single Responsibility Principle)
- 하나의 클래스가 **한 가지 일만** 해야 한다.
- **하나의 이유로만 변경**되어야 한다.

