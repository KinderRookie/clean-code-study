# 1. 응집도 
클래스 내부의 데이터와 메서드들이 하나의 논리적인 책임을 중심으로 서로 밀접하게 관련돼 있는 정도
### 응집도가 높다?
- 메서드 대부분이 공통된 인스턴스 변수에 접근
- 클래스 내부가 하나의 목적/개념을 중심으로 구성되어 있음
- 책임이 명확함
```csharp
public class Stack
{
    private int _topOfStack = 0;
    private List<int> _elements = new List<int>();

    public int Size() => _topOfStack;

    public void Push(int element)
    {
        _topOfStack++;
        _elements.Add(element);
    }

    public int Pop()
    {
        if (_topOfStack == 0)
            throw new InvalidOperationException("Stack is empty.");

        int element = _elements[--_topOfStack];
        _elements.RemoveAt(_topOfStack);
        return element;
    }
}
```

다음과 같은 시나리오가 있다. 
- 큰 함수 내부에 있는 부분을 다른 함수로 빼내려고 하는데, 이 부분은 큰 함수 내부에 정의된 변수를 4개나 사용하고 있다.
	**변수들을 클래스 인스턴스 변수로 승격한다.**
	새 함수는 인수가 필요 없어진다.
- 이제 클래스가 응집력을 잃는다. 일부 함수만 사용하는 인스턴스 변수가 늘어났기 때문이다. 
	이제 그런 함수들을 독자적인 클래스로 분리한다. 

# 2. 변경
변경하기 쉬운 설계를 따라야 한다. 
```csharp
public class Sql {
    public Sql(String table, Column[] columns)
    public String create()
    public String insert(Object[] fields)
    public String selectAll()
    public String findByKey(String keyColumn, String keyValue)
    public String select(Column column, String pattern)
    public String select(Criteria criteria)
    public String preparedInsert()
    // private 메서드들...
}
```
- **SRP 위반**: SQL 생성과 관련된 모든 책임이 하나의 클래스에 있음 
- 새로운 SQL문을 추가하거나 수정하려면 이 클래스를 계속 수정해야 함
- insert(), select(), findByKey()는 다른 동작인데 같은 클래스에 묶여 있음
#### 역할별로 클래스 나누기: 변경이 필요한 지점에만 새로운 클래스를 추가할 수 있도록...

### STEP1.
```csharp
public abstract class Sql
{
    protected readonly string Table;
    protected readonly Column[] Columns;

    protected Sql(string table, Column[] columns)
    {
        Table = table;
        Columns = columns;
    }

    public abstract string Generate();
}
```

### STEP2. 
```csharp
public class CreateSql : Sql
{
    public CreateSql(string table, Column[] columns)
        : base(table, columns) { }

    public override string Generate()
    {
        var cols = string.Join(", ", Columns.Select(c => $"{c.Name} {c.Type}"));
        return $"CREATE TABLE {Table} ({cols});";
    }
}
```

```csharp
public class InsertSql : Sql
{
    private readonly object[] _values;

    public InsertSql(string table, Column[] columns, object[] values)
        : base(table, columns)
    {
        _values = values;
    }

    public override string Generate()
    {
        var columnList = string.Join(", ", Columns.Select(c => c.Name));
        var valueList = string.Join(", ", _values.Select(v => $"'{v}'"));
        return $"INSERT INTO {Table} ({columnList}) VALUES ({valueList});";
    }
}
```

```csharp
public class Column
{
    public string Name { get; }
    public string Type { get; }

    public Column(string name, string type)
    {
        Name = name;
        Type = type;
    }
}
```

요런 느낌으로 분리....

